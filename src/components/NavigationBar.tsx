import { Box, Flex, Text } from "@chakra-ui/react";

export default function NavigationBar(): JSX.Element {
  return (
    <Box>
      <Flex p={4}>
        <Text>Hej</Text>
      </Flex>
    </Box>
  );
}
